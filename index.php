<?php

/* codigo de ejemplo, no es nada real o funcional pragmaticamente hablando
It's just placeholder code, not meant to be taken serious. */
function compareArraysBasic(array $arr1, array $arr2)
{
    if ($arr1 != $arr2) {
        echo "the arrays aren't the same";
    }
    echo "the arrays are the same!";
}

